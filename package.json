{
  "private": true,
  "scripts": {
    "dev": "npm --prefix fe+convex run dev",
    "build": "npm --prefix fe+convex run build",
    "start": "npm --prefix fe+convex run start",
    "lint": "npm --prefix fe+convex run lint"
  },
  "dependencies": {
    "tw-animate-css": "^1.4.0"
  }
}
